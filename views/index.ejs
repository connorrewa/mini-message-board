<html>
    <body>
        <h1><%= title %></h1>
        <ul>
            <% for(let i = 0; i < messages.length; i++) { %>
            <li>
                <%= messages[i].username %>:&nbsp;<%=messages[i].message%>
                &nbsp;-&nbsp; <%= format(new Date(messages[i].date_added),
                "MM/dd/yyyy HH:mm") %>
                <a href="/<%= i+1 %>"><button>View Message</button></a>
                <%= console.log(messages[i]); %>
            </li>
            <% } %>
            <a href="/new">Add new message!</a>
        </ul>
    </body>
</html>
